# BlavaClub — React (Vite) scaffold

This repo has been scaffolded into a minimal React app using Vite.

Quick start:

```bash
npm install
npm run dev
```

Build:

```bash
npm run build
npm run preview
```

Files created: `package.json`, `vite.config.js`, `index.html`, `src/`.
